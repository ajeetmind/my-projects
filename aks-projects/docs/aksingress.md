# Currently ingress controller is deployed on K8s to manage external traffics on aks controller

## Dynamic ip , Needs to set static IP for ingress controller.

```
currently ingress controller is using dynamic ip , Needs to set static IP for ingress controller on it.
```

## Certificate.

```
Near future we need to use TLS certificates on ingress controller. so that it will support HTTPS traffics which will be secure. 
```